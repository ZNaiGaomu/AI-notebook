# AI 记录本（ai-notebook）

本地优先的 Obsidian 插件：用**可版本化能力蓝图（Blueprint）**定义每本记录本的能力；同一插件可运行多个互不干扰的 AI 记录本。条目以 Markdown 落盘，**功能版本回滚不会改写用户笔记内容**。

| | |
|---|---|
| **当前版本** | `0.7.0` |
| **插件 ID** | `ai-notebook` |
| **仓库** | [ZNaiGaomu/AI-notebook](https://github.com/ZNaiGaomu/AI-notebook) |
| **最低 Obsidian** | `1.5.0` |
| **规格文档** | [`docs/V1-蓝图定稿.md`](docs/V1-蓝图定稿.md) |
| **本版说明** | [`docs/RELEASE-v0.7.0.md`](docs/RELEASE-v0.7.0.md) |

---

## 目录

1. [只要这一小份就能用](#只要这一小份就能用)
2. [0.7.0 亮点（第七版）](#070-亮点第七版)
3. [功能总览](#功能总览)
4. [安装与更新](#安装与更新)
5. [快速上手](#快速上手)
6. [浮层助手（写笔记）](#浮层助手写笔记)
7. [改功能（改蓝图）](#改功能改蓝图)
8. [历史版本：本内蓝图 + 插件安装包](#历史版本本内蓝图--插件安装包)
9. [AI 与多模型路由](#ai-与多模型路由)
10. [语音录入](#语音录入)
11. [附件、历史与路径设置](#附件历史与路径设置)
12. [手机与收件箱](#手机与收件箱)
13. [命令一览](#命令一览)
14. [开发与打包](#开发与打包)
15. [版本说明](#版本说明)
16. [隐私与数据边界](#隐私与数据边界)

---

## 只要这一小份就能用

**结论：只需要 `release/ai-notebook/` 里的文件，不必下载/解压整个仓库。**

仓库里还有 `src/`、`docs/`、`tests/` 等，那是**开发与构建**用的。插件运行时已经打进 `main.js`，**不依赖** GitHub 仓库、不依赖源码目录，也和别人的密钥/笔记无关。

### 最终只要这 3 个文件

```text
.obsidian/plugins/ai-notebook/
  ├── manifest.json   # 插件身份与版本
  ├── main.js         # 全部逻辑（已打包）
  └── styles.css      # 样式
```

| 你拷什么 | 能否正常运行 |
|----------|----------------|
| **仅** `release/ai-notebook/`（上面 3 个文件） | ✅ 可以 |
| 整个仓库（源码 + 文档 + 测试） | 能 clone，但**装插件时用不到**，纯多余 |
| Tag 的 Source code 整夹当插件目录 | ❌ 不行（须进入 `release/ai-notebook/`） |

### 最短安装步骤

1. 取得安装文件（任选其一）：
   - **推荐**：Releases 里的 `ai-notebook-vX.Y.Z.zip`（解压即是这 3 个文件）
   - 或：仓库 / 本机路径里的 [`release/ai-notebook/`](release/ai-notebook/)
   - 或：Tag 的 Source code zip → **只进入**其中的 `release/ai-notebook/`
2. 复制到：`<你的库>/.obsidian/plugins/ai-notebook/`
3. 设置 → 社区插件 → 关闭受限模式 → 启用 **AI 记录本**
4. （可选）在插件设置里自己填 Base URL、API Key、模型

**更新**：只覆盖这 3 个文件；**不要删** 同目录下的 `data.json`（你自己的 Key 与设置）。

---

## 0.7.0 亮点（第七版）

相对 `0.6.0`，第七版重点交付原生 Android App，并统一条目命名、手机本地来源管理和语音重新转写体验。

| 能力 | 说明 |
|------|------|
| **原生 Android App** | Android 10+；文字、录音和文件定向写入，离线待发送、垃圾箱与二维码连接 |
| **文件防重复入队** | 加入待发送后立即清空选择；处理期间禁止重入；同批 URI 去重 |
| **手机来源双通道** | 同时保留 App 私有副本与原始 `content://` URI，分别提供打开入口 |
| **最近记录多选清理** | 可仅删手机副本，或同时移除手机本地最近记录；不会删除电脑 vault 文件 |
| **队列删除保护** | 待发送或垃圾箱仍引用来源时默认取消，必须明确确认后才会一并移除引用 |
| **稳定条目显示名** | AI 助手、附件归属和列表统一使用 `items/*.md` 文件名，不受可编辑标题影响 |
| **语音重新转写** | 阅读视图可重试 STT；内部标记改为 Obsidian 隐藏注释，并兼容旧标记 |
| **移动桥严格定向** | 显式记录本 ID 失效时不再静默写入默认本；离线语音缓存流程不再误报失败 |

### 第七版下载

[GitHub Releases](https://github.com/ZNaiGaomu/AI-notebook/releases/latest) 同时提供两个独立附件：

- `ai-notebook-v0.7.0.zip`：Obsidian 插件安装包，只含 `manifest.json`、`main.js`、`styles.css`
- `suji-v0.7.0-release-unsigned.apk`：Android 安装包；当前为未签名 Release 构建，适合侧载测试

> Tag 的 Source code ZIP 是完整开发源码，不是插件安装包。Android 源码位于 `release/app/`。

---

## 功能总览

### 记录本与数据

- **多实例记录本**：独立目录、蓝图版本、条目列表、收藏柜
- **模板**：文献 / 灵感 / 会议 / 收藏向 / 空白
- **Markdown 存储**：`items/*.md`，可被 Obsidian 搜索、同步、备份
- **手工条目吸收**：在 `items/` 下手工新建 `.md` 会自动成为条目；非 Markdown 文件会生成引用型条目
- **列表 / 表格 / 看板**：由蓝图 `views` 驱动，可筛选排序（仅一种视图时不显示多余模式按钮）

### 蓝图与版本

- 能力由 `blueprints/vNNNN.json` 描述
- **另存蓝图版本**、历史恢复、Diff 确认
- 回滚蓝图**不改写**已有条目正文（未映射字段保留）

### AI 能力

| 模式 | 对象 | 典型用途 |
|------|------|----------|
| **助手** | 当前（或选中的）**条目** | 改正文、插图/视频、描述画面、整理内容 |
| **改功能** | **整本记录本的蓝图** | 加字段、改视图、开收藏柜等「小插件」级能力 |
| **整理** | 条目 / 收件箱 | 杂乱文本 → 字段 + 分层正文 |
| **语音** | 录音 | Whisper / 兼容 transcriptions，或聊天多模态听音频 + 可选润色 |

### 插件安装包历史（0.3.0）

- 多 GitHub 来源拉取 Tags / Release / 默认分支 ZIP
- 本地备份列表 + 一键切换
- **永不**因拉取而改当前运行包；切换不碰 `data.json` 与笔记

### 附件与收藏

- **附件管理**（0.6.0）：分类保存、正文嵌入预览、删除互不影响
- **收藏柜**：可选链接/文件收藏，与普通上传分离

### 跨端

- **原生 Android App（0.7.0）**：Android 10+；文字、录音、文件、离线队列、垃圾箱、最近记录多选清理
- 手机本地来源可分别打开 App 私有副本与原始文件 URI；清理操作不触碰电脑 vault
- 收件箱 `AI Inbox/pending`（及 `files/` 二进制）→ 可选跨本 / 指定条目整理
- 手机端可选择记录本或记录本下已有条目：选记录本=新建条目，选已有条目=追加正文末尾
- 手机端可新建记录本、在记录本下新建命名条目
- 普通手机文件上传 → 附件目录 + 正文嵌入；「仅收件箱」保留原文件待整理
- Tailscale 虚拟局域网手机入口（推荐稳定）
- 局域网手机网页入口
- Cloudflare / ngrok 等公网隧道（可选）

---

## 安装与更新

> 核心就一句：**只装 `release/ai-notebook` 三文件即可**。

### 方式 A：Release 安装包（推荐）

1. [Releases](https://github.com/ZNaiGaomu/AI-notebook/releases) → 下载 **`ai-notebook-vX.Y.Z.zip`**（附件，不是 Source code）
2. 解压得到 `manifest.json`、`main.js`、`styles.css`
3. 放入 `<库>/.obsidian/plugins/ai-notebook/`
4. 启用插件 →（可选）配置自己的 API

### 方式 B：只用仓库里的 `release/ai-notebook`

```text
release/ai-notebook/manifest.json
release/ai-notebook/main.js
release/ai-notebook/styles.css
```

### 方式 C：从 Tag 的 Source code zip

解压后进入 **`release/ai-notebook/`**，再只拷上述 3 个文件。

### Android App 安装

1. 前往 [Releases](https://github.com/ZNaiGaomu/AI-notebook/releases/latest)，下载 Android APK 独立附件
2. Android 10 及以上允许安装来自浏览器/文件管理器的应用后侧载 APK
3. 在 Obsidian 插件中启动手机入口，复制含 `?t=` 的完整链接或二维码
4. App 设置页粘贴链接/扫码并测试连接

Android 权限与边界：

- 网络权限：连接电脑端 Obsidian 桥服务
- 录音权限：原生语音采集
- 相机权限：扫描连接二维码
- 手机删除只影响 App 私有副本和手机本地记录，不删除电脑端 Obsidian vault 文件
- 原始文件 URI 能否长期打开取决于 Android 文件提供方；App 私有副本作为可靠兜底

### 更新

- 只覆盖 `manifest.json`、`main.js`、`styles.css`
- **保留** `data.json`
- 建议禁用再启用插件，或重启 Obsidian

### 从源码构建（仅开发需要）

```bash
git clone https://github.com/ZNaiGaomu/AI-notebook.git
cd AI-notebook
npm install && npm test && npm run package
# 产物：release/ai-notebook/ 与 release/history/vX.Y.Z/
```

---

## 快速上手

1. Ribbon 点击 **AI 记录本**，或命令「打开 AI 记录本」
2. **新建记录本** → 选模板
3. 用 **语音录入**、浮层 **助手**、手机入口，或直接在 `items/` 文件夹手工新建条目
4. 右下角 **助手** → 上传图/视频 →「插入正文并描述」
5. 需要改结构能力时切到 **改功能**，确认 Diff 后应用
6. **历史版本** → 可管本内蓝图，或配置 GitHub 来源切换插件安装包

---

## 浮层助手（写笔记）

### 窗口

- 默认 **收起** 为右下角按钮
- 可拖标题栏、右下角缩放；**尺寸/位置会记住**
- 内含：助手 / 改功能、历史、新对话、记录本与条目选择

### 写入规则（摘要）

- **默认**上传仅供参考，不自动进笔记
- 明确说「插入正文 / 放进笔记」→ `embed_in_body`（正文 `![[path]]`）
- 明确说「收藏柜」→ 才进收藏柜
- **本轮附件**只含本轮上传

### 历史（按条目）

- 助手历史绑定 **当前条目**
- 进入条目默认 **接续最近会话**
- **新对话**会真正新建线程

### 追加发送

- **排队**：上一条结束后自动发下一条
- **引导**：补充到当前进行中的请求

---

## 改功能（改蓝图）

- 对象是整本记录本的 **功能配置**，不是某一条笔记正文
- AI 生成完整蓝图 JSON → 校验 → **Diff 确认** → 提交新版本
- 工具栏 **「另存蓝图版本」** 会把当前配置手动提交进 **本内功能历史**
- **「恢复此本内版本」** 会再提交一条指向旧配置的新版本（历史可追溯）

---

## 历史版本：本内蓝图 + 插件安装包

打开工具栏 **历史版本**，两条独立时间线：

### 本内功能历史

- 只改当前记录本的 `blueprints/`
- 与「另存蓝图版本」同一条线
- **不改** items 笔记正文

### 插件整体历史

三条线互不混用：**本地备份**、**每个 GitHub 来源各自列表**、**本机 manifest 版本号**。
「仅版本号相同」**不等于**正在使用某个远程包；只有你点过 **使用 / 切换** 成功后，才标记 **当前使用**。

#### 1）GitHub 版本来源（多行，仿 AI 服务商）

1. 填写 **显示名称** + **仓库链接**（如 `https://github.com/ZNaiGaomu/AI-notebook`）→ 添加
2. **拉取版本**（只更新该来源的可选列表，**绝不改当前运行包**）
3. **打开版本** → 看说明与拉取方式 → **下载到本地** / **下载并使用**

拉取按**优先级**（前一通道有结果就停，不混通道）：

| 优先级 | 方式 | 卡片标注 |
|--------|------|----------|
| **1** | **Release 安装包附件**（如 `ai-notebook-*.zip`） | `Release 附件` |
| **2** | 无可用 Release 包时 → **Tags 源码 zip** | `Tags 源码包` |
| **3** | Tags 也没有 → **Code → Download ZIP**（默认分支最新，通常 1 条） | `Code Download ZIP` |

- 每条版本会标明 **拉取方式** 与状态：`未下载` / `已下载（未应用）` / `当前使用（已从本条应用）`
- 下载路径（同号不同源隔离）：

```text
.obsidian/plugins/ai-notebook/package-archive/by-source/{sourceId}/vX.Y.Z/
```

#### 2）本地运行备份（独立列表）

- **立即备份当前安装包** → 下方出现卡片 → **切换到此本地备份**
- 路径：`package-archive/vX.Y.Z/`（与 GitHub 来源分开）
- 启动自动备份：若本地**已有当前版本**则跳过，避免冗余
- 只有从**某条本地备份**切换成功，才在该卡片标「当前使用」

#### 当前使用 vs 本机版本号

| 显示 | 含义 |
|------|------|
| 本机 manifest 版本 | 当前装在插件目录的版本号（事实） |
| 当前使用 | 你最后一次从历史页 **应用成功** 的来源 + 版本（未应用过则明确写「尚未应用」） |

#### 切换安全边界

- 只会覆盖：`main.js` / `manifest.json` / `styles.css`
- **不修改** `data.json`，**不改** vault 笔记
- 切换后请 **禁用再启用** 本插件，或重启 Obsidian

---

## AI 与多模型路由

设置 → **AI 服务商与模型** → **用途 → 服务商（有序回退）**：

| 用途 | 典型用途 |
|------|----------|
| 改功能 / 规划 | 蓝图编译 |
| 整理 / 助手 | 条目助手、看图、整理 |
| 语音转写 | Whisper 等 STT |

每个用途可配 **顺序 1 / 2 / 3**。支持 OpenAI 兼容 Base URL + API Key；可拉模型列表、测试连通。

---

## 语音录入

- 录音容器：auto / wav / webm / m4a / mp3（与浏览器协商）
- 可选转码 16k mono WAV 再 STT
- 用途链上多模型 fan-out；transcriptions 失败可 chat 听音频回退
- 转写后润色（可开关、可配 prompt）
- 命令：**语音录入到记录本**、**诊断语音转写能力**

---

## 附件、历史与路径设置

| 设置项 | 说明 |
|--------|------|
| 记录本根目录 | 默认 `AI Notebooks` |
| 附件根目录 | 默认 `attachments/ai-notebook` |
| **对话上传保存位置** | 默认 `{附件根}/chat-uploads` |
| **对话附件保留** | 默认永久 |
| 手机收件箱 | 默认 `AI Inbox` |

---

## 手机与收件箱

1. **定向写入**：手机端先选记录本，再选“新建条目”或已有条目；已有条目会追加到正文末尾
2. **手机新建记录本 / 条目**：无需回电脑端，也能创建记录本和命名条目
3. **同步收件箱**：手机写到 `AI Inbox/pending`，电脑端「处理收件箱」
4. **Tailscale 虚拟局域网**：电脑和手机登录同一 Tailscale 后，复制 `100.x` 链接即可打开
5. **局域网网页**：电脑启动入口，同 Wi‑Fi 打开链接写字/录音
6. **公网隧道**：Cloudflare / ngrok 等（需本机工具）

---

## 命令一览

| 命令 | 说明 |
|------|------|
| 打开 AI 记录本 | 打开上次/选择记录本 |
| 新建记录本 | 从模板创建 |
| 选择并打开记录本 | 切换实例 |
| 用语言改功能 | AI 产蓝图 + Diff |
| 手机速记 / 处理收件箱 | 收件箱链路 |
| AI 整理当前条目 | 结构化当前条 |
| 语音录入到记录本 | 转写并写入 |
| 诊断语音转写能力 | 检查 Provider |
| 显示/启动/停止手机网页入口 | Tailscale / 局域网 / 公网入口 |
| 生成任意网络手机链接 | 公网隧道 |

---

## 开发与打包

```bash
npm install
npm run dev          # watch，自动同步 release/ai-notebook
npm run build        # 生产构建 + 同步 release
npm test
npm run package      # 构建 + release + history/vX.Y.Z
```

主要目录：

```text
src/                 源码
release/ai-notebook  安装用运行文件
release/history/     历史安装包
docs/                规格与说明
```

---

## 版本说明

### 0.5.0（第五版）

- 手机端目标选择升级为“记录本 + 可选条目”：选记录本时新建条目，选已有条目时追加到正文末尾
- 手机端支持新建记录本，并可选择空白 / 文献 / 灵感 / 会议 / 收藏向模板
- 手机端支持在所选记录本下新建可命名条目，创建后自动作为后续发送目标
- 文字、语音转写、文件上传均支持追加到已有条目；待发送队列保留记录本和条目目标

### 0.4.0（第四版）

- 手工 `.md` 笔记放入记录本 `items/` 后自动补齐为 AI 条目
- 图片 / PDF / Canvas / 白板等非 Markdown 文件自动生成引用型 Markdown 条目
- 新建记录本弹窗加固：默认模板名、原生输入、自动聚焦、回车创建
- 手机入口新增 Tailscale 虚拟局域网连接方式，并保留普通局域网与公网隧道

### 0.3.1

- 历史版本逻辑打磨：拉取 **Release → Tags → Code** 优先级（不混通道）
- 「当前使用」仅在真正应用后标记；去掉「版本号相同即当前」误标
- 每条远程版本标注拉取方式；本地备份与远程来源列表更清晰

### 0.3.0（第三版）

- GitHub 多来源安装包：Tags / Release / Code ZIP 与一键切换
- 本地备份列表可切换；启动同版本跳过冗余自动备份
- 本内历史文案挂钩「另存蓝图版本」
- 语音流水线与用途路由增强
- 界面：隐藏单视图「列表」标签；移除快速捕获

### 0.2.0

- 浮层助手真写入、媒体嵌入、多模型回退、对话附件
- 按条目会话；用途级路由链

### 0.1.x

- 多实例、模板、条目 CRUD、蓝图版本、改功能、收藏柜
- 语音雏形、收件箱、手机网页入口与公网隧道
- Provider 多服务商与基础路由

---

## 隐私与数据边界

### 开源仓库里有什么 / 没有什么

| 内容 | 是否在 GitHub 仓库中 |
|------|----------------------|
| 源码、`main.js` 安装包 | ✅ 有（供他人自行部署） |
| 你的 API Key | ❌ 无（只在本机 `data.json`，已 gitignore） |
| 你的笔记 / 对话 / 上传文件 | ❌ 无（只在你自己的 Obsidian 库） |

### 数据何时会离开本机

- 配置了 AI 并调用时 → 请求发往**你填写的**服务商
- 使用「拉取版本 / 下载安装包」时 → 请求 GitHub / jsDelivr 等公开元数据与 zip（**不上传**你的笔记）

### 使用者注意

- 自行准备 OpenAI 兼容 Base URL 与 API Key
- 不要把含真实 Key 的 `data.json` 提交到公开仓库
- 助手写入与插件切换都会改本地文件，请备份库

---

## 许可

见仓库 `LICENSE`。

问题与建议请到 [Issues](https://github.com/ZNaiGaomu/AI-notebook/issues)。
